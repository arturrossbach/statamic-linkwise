/node_modules
/vendor
composer.lock
package-lock.json
.phpunit.result.cache
# Statamic-addon convention: ship built frontend assets (resources/dist/)
# so `composer require` installs work without an npm build step on the
# user's server. We don't gitignore /resources/dist — it's part of the
# release artifact. CI rebuilds this on every push to master.
/docs/.vitepress/dist
/docs/.vitepress/cache
.claude/
CLAUDE.md
docs/ARCHITECTURE_REVIEW.md
docs/SISTER_AUDIT_2026_05_17.md
*.iml
.idea/
/test-results
docs/internal/

# Internal planning, competitor research & audits — never public
LINK-WHISPER-FEATURE-INVENTORY.md
LINKBOSS-FEATURE-INVENTORY.md
LINKWISE-V2-MASTER-PLAN.md
V1X-POLISH-AUDIT.md
MARKETPLACE_CHECKLIST.md
SMOKE_QUEUE.md
TAB-GAP-ANALYSIS.md
NoteGPT_TRANSCRIPT_*.txt
docs/MARKETPLACE_LISTING.md
docs/MULTISITE_AUDIT.md
docs/CODE_REVIEW_*.md

# Frequency-Words raw lists — too large for the repo (3-5MB each × 9 langs).
# Pre-stemmed JSONs (`resources/data/frequency-stems-*.json`) are checked in
# as the runtime artifact. To rebuild from scratch:
#   1. Download `<lang>_50k.txt` from github.com/hermitdave/FrequencyWords
#   2. Save as `resources/data/_raw_<lang>_50k.txt`
#   3. Run `php artisan linkwise:build-frequency-stems <lang>`
/resources/data/_raw_*_50k.txt
